'条件语句：实现多条件的判断：'
if ...:
elif...:
else :
...


'循环语句：'
while  # 代码块会一直循环执行，直到布尔表达式的值为布尔假
for - else   # for循环正常执行完,执行else;若for循环中有break,则不执行else
while - else # 与上相似
continue  # 终止本轮循环并开始下一轮循环


列表list[]
元组tupple()
字符转str'' 
集合set  # 无序，可修改，不重复 {1, 1, 2, 3}相当于是{1, 2, 3}
字典dic  # 无序，可修改，key不可重复 例:{1: '赞', 2: '棒', 3: '牛'}
'注：type({})，结果会是dic; type(set({}))结果才是set'
